[project]
name = "afroid-vector-store"
version = "1.0.0"
description = "Afroid Vector Store — Embedding generation and similarity search via pgvector"
requires-python = ">=3.12"
dependencies = [
    "afroid-shared",
    "langchain-google-genai>=2.0.0",
]

[tool.uv.sources]
afroid-shared = { workspace = true }
