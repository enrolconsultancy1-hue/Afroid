[project]
name = "afroid-notification"
version = "1.0.0"
description = "Afroid Notification Service — Email and push notifications"
requires-python = ">=3.12"
dependencies = [
    "afroid-shared",
]

[tool.uv.sources]
afroid-shared = { workspace = true }
